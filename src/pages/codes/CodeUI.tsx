import { makeStyles, createStyles } from "@material-ui/core";

const CodeUI = makeStyles(() =>
  createStyles({
    tableContainer: {
        width: '100%',
        height: '100%',
    }
  }),
);

export default CodeUI;